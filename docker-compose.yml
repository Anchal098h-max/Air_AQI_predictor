version: "3.9"

services:
  air-quality-api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: air-quality-api

    ports:
      - "8000:8000"

    restart: unless-stopped

    environment:
      PYTHONUNBUFFERED: "1"

    volumes:
      - .:/app